PORT=3001
# Local dev: `docker compose up db -d` starts this Postgres instance.
# Production (e.g. Render): use a hosted Postgres such as https://neon.tech (free tier).
DATABASE_URL="postgresql://leadgen:leadgen@localhost:5432/leadgen"
JWT_SECRET="change-this-to-a-random-secret"
CORS_ORIGIN="http://localhost:5173"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
FROM_EMAIL="noreply@prospectpro.com"

# ScrapeGraphAI scraper service (see scraper/ directory)
SCRAPER_URL="http://localhost:8000"
SCRAPER_TIMEOUT_MS=600000
